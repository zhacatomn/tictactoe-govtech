const PLAYER_TOKENS = ["X", "O"];
const EMPTY_SPACE = "";
const NUM_ROWS = 3;
const NUM_COLS = 3;
const ROOM_ID_LENGTH = 5;

module.exports = {
  PLAYER_TOKENS,
  EMPTY_SPACE,
  NUM_ROWS,
  NUM_COLS,
  ROOM_ID_LENGTH,
};
